# LFB Mobilisations — Join-Quality Memo

> Auto-generated by `src/data/build_mobilisations.py`.

Reports how the mobilisation parquet joins back to the canonical incident artefact (`lfb_canonical_2024_2026.parquet`) on `IncidentNumber`. Numbers below are the evidence base for any §5/§6 claim that uses appliance-level fields (deployed-from station, attendance time, pump order); reproduce by re-running the build script.

## Mobilisation-side coverage

- **Mobilisation rows in window**: 439,974.
- **Rows whose IncidentNumber matches a canonical incident**: 438,211 (99.60\%).
- **Distinct mobilisation IncidentNumbers**: 279,428.

## Incident-side coverage

- **Canonical incidents in 2024--2026**: 293,646.
- **Canonical incidents with at least one mobilisation row**: 278,043 (94.69\%).
- **Acceptance floor (all incidents)**: 93\% (validation-check threshold). Gap is dominated by canonical rows with no `FirstPumpArriving_AttendanceTime` -- i.e.\ incidents where no pump was dispatched / arrived (cancelled calls, false alarms not requiring response). They are correctly absent from the mobilisation file and should not be treated as join failures.

## First-arriving-pump cross-check vs canonical `FirstPumpArriving_AttendanceTime`

This is the strict denominator for response/deployment claims: among canonical incidents where a first pump actually arrived, a mobilisation row with `is_first_arriving_pump = True` should exist and its `attendance_time_seconds` should match the canonical's `FirstPumpArriving_AttendanceTime`.

- Canonical incidents with a recorded first-pump time: 278,468.
- Of those, share with at least one mobilisation row flagged `is_first_arriving_pump`: 99.85\%.
- Acceptance floor (dispatched incidents): 99\% (validation-check threshold).
- Median absolute diff (mob first-arriving min vs canonical FirstPumpArriving_AttendanceTime), seconds: 0.0.
- Share within 5\,s of canonical: 100.00\%.
- Share within 30\,s of canonical: 100.00\%.

Interpretation: the canonical's `FirstPumpArriving_AttendanceTime` field is itself derived from the same upstream operational system as `PerformanceReporting=1` rows here, so values are expected to be identical or very close. Diffs above 30\,s suggest either a data-vintage mismatch between the two open-data releases or a denominator difference (multiple performance flags per incident); inspect cases before publishing claims.

## Pumps per incident (mobilisation rows per IncidentNumber)

- Median: 1
- 90th percentile: 2
- Max: 52

Note: incidents in the canonical with `NumPumpsAttending` should approximately equal the count of mobilisation rows for that IncidentNumber. Large divergences are expected for sustained incidents (re-mobilisations, relief crews) and for incidents whose `NumPumpsAttending` field was set at a different snapshot than the mobilisation-records release.
